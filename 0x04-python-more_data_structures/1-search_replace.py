#!/usr/bin/python3
def search_replace(my_list, search, replace):
    new_list = my_list[:]
    for i in range(len(my_list)):
        if value == search
        else value for value in my_list
        return (new_list)
